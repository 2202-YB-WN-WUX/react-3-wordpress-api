const Hero = () => {
    return (
        <div id="hero">
            <h1>Burning Man</h1>
            <p>Your #1 resource for good times</p>
        </div>
    )
}

export default Hero

// Show all the artists on the screen using map
// No need to show content or excerpt, show artist name and image.
// Grab the data using Axois
// Style it up a little bit